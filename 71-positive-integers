public List<Integer> positiveIntegers(List<Integer> numbers) {
    List<Integer> positiveIntList = new List<Integer>();
    for (Integer i=0; i< numbers.size(); i++) {
        if (numbers.get(i) > 0) {
            positiveIntList.add(numbers.get(i));
        }
    }
    return positiveIntList;
}
