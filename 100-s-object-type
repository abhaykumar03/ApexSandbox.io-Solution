public Boolean isTypeAccount(sObject record)
{
Account acc = new Account(sObject);
Boolean isAccount = TypeChecker.isTypeAccount(acc);
if(isAccount == true)
{
    return true;
}
else{
    return false;
}
}